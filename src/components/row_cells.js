import React, { Component } from 'react';


//function receives props telling it to make X objects per array
export default function rowCells(props)=>{
    const num = props.number;
    return(
        <div></div>
    )
}